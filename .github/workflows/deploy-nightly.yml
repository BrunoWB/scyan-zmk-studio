name: Deploy Nightly to GitHub Pages

on:
  push:
    branches: [nightly]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: nightly-pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[skip ci]')"
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build (nightly base path)
        run: npm run build
        env:
          VITE_BASE_PATH: /nightly/scyan-zmk-studio/

      - name: Ensure .nojekyll present
        run: touch dist/.nojekyll

      - name: Clone brunowb.github.io (full, not shallow)
        run: |
          git clone \
            "https://x-access-token:${{ secrets.GH_PAT_PAGES }}@github.com/BrunoWB/brunowb.github.io.git" \
            /tmp/user-site

      - name: Wipe and replace nightly subfolder
        run: |
          cd /tmp/user-site
          # Remove entire nightly/scyan-zmk-studio subtree from git index AND disk
          git rm -rf nightly/scyan-zmk-studio/ --ignore-unmatch
          mkdir -p nightly/scyan-zmk-studio
          cp -r $GITHUB_WORKSPACE/dist/. nightly/scyan-zmk-studio/
          # Force-add everything including assets/
          git add -f nightly/scyan-zmk-studio/

      - name: Commit and push
        run: |
          cd /tmp/user-site
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          echo "=== Staged files ==="
          git diff --cached --name-only | head -20
          git diff --cached --quiet && echo "No changes" && exit 0
          git commit -m "chore(nightly): deploy scyan-zmk-studio ${{ github.sha }} [skip ci]"
          git push origin main
